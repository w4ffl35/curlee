/* Generated by curlee build --target freestanding-c. */
/* Verified Curlee program compiled to freestanding C. */

#include <stdint.h>

static uint32_t curlee_inner(uint32_t* r, int64_t x);
static uint32_t curlee_outer(uint32_t* r, int64_t x);
uint32_t curlee_main(void);

static uint32_t curlee_inner(uint32_t* r, int64_t x)
{
    uint32_t v = (*(r));
    return v;
}

static uint32_t curlee_outer(uint32_t* r, int64_t x)
{
    int64_t y = (x + 1);
    return curlee_inner(r, y);
}

uint32_t curlee_main(void)
{
    /* unit binding reg */
    return curlee_outer((void*)(uintptr_t)(0x5000), 10);
}

