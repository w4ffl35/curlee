// SPDX-License-Identifier: MIT
//
// Issue #278 expected-fail: arrays cannot be return types in the MVP (the
// freestanding target has no array value-copy convention). Expected error:
//   "array types are only supported as local 'let' bindings in the MVP (not as
//    return types)"

fn f() -> [Int; 8] {
  let q: [Int; 8] = [0; 8];
  return q;
}

fn main() -> Int {
  return 0;
}
