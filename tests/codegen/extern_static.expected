/* Generated by curlee build --target freestanding-c. */
/* Verified Curlee program compiled to freestanding C. */

#include <stdint.h>

uint64_t mb2_info_addr = 0;
int64_t handoff_flag = 0;
uint8_t boot_scratch[16] = {0};

static uint64_t curlee_mb2_info_addr_get(void);
int64_t curlee_main(void);

static uint64_t curlee_mb2_info_addr_get(void)
{
    return mb2_info_addr;
}

int64_t curlee_main(void)
{
    uint64_t v = curlee_mb2_info_addr_get();
    int64_t f = handoff_flag;
    boot_scratch[0] = 0xAB;
    uint8_t b = (boot_scratch)[0];
    if ((v == mb2_info_addr))
    {
        return (f + b);
    }
    return 0;
}

