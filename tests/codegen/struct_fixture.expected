/* Generated by curlee build --target freestanding-c. */
/* Verified Curlee program compiled to freestanding C. */

#include <stdint.h>

typedef struct curlee_Point curlee_Point;

typedef struct curlee_Point {
    int64_t x;
    int64_t y;
} curlee_Point;

int64_t curlee_main(void);

int64_t curlee_main(void)
{
    curlee_Point p = ((curlee_Point){ .x = 1, .y = 2 });
    curlee_Point q = ((curlee_Point){ .x = 10, .y = 20 });
    int64_t sum = ((((p).x + (p).y) + (q).x) + (q).y);
    return sum;
}

