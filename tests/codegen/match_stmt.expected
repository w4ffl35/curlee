/* Generated by curlee build --target freestanding-c. */
/* Verified Curlee program compiled to freestanding C. */

#include <stdint.h>

typedef struct curlee_Color curlee_Color;

enum curlee_Color_tag {
    curlee_Color_Red = 0,
    curlee_Color_Green = 1,
    curlee_Color_Blue = 2,
};

typedef struct curlee_Color {
    enum curlee_Color_tag tag;
    union {
        int64_t Blue;
    } payload;
} curlee_Color;

int64_t curlee_main(void);

int64_t curlee_main(void)
{
    curlee_Color c = ((curlee_Color){ .tag = curlee_Color_Red });
    if ((c).tag == curlee_Color_Red)
    {
        return 1;
    }
    else if ((c).tag == curlee_Color_Green)
    {
        return 2;
    }
    else if ((c).tag == curlee_Color_Blue)
    {
        int64_t v = (c).payload.Blue;
        return v;
    }
    return 0;
}

