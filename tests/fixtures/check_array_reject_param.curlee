// SPDX-License-Identifier: MIT
//
// Issue #278 expected-fail: arrays cannot cross function boundaries in the
// MVP (no slices/borrows yet); a parameter of array type is rejected.
// Expected error:
//   "array types are only supported as local 'let' bindings in the MVP (not as
//    parameters)"

fn f(q: [Int; 8]) -> Int {
  return q[0];
}

fn main() -> Int {
  return 0;
}
