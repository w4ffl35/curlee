/* Generated by curlee build --target freestanding-c. */
/* Verified Curlee program compiled to freestanding C. */

#include <stdint.h>

static uint8_t rx[16] = {0};

static int64_t curlee_probe(void);
int64_t curlee_main(void);

static int64_t curlee_probe(void)
{
    {
        int64_t rx_addr = (int64_t)(uintptr_t)(rx);
        uint8_t q[16] = {0};
        q[0] = 1;
        int64_t qa = (int64_t)(uintptr_t)(q);
        int64_t acc = (((q)[0] + qa) + rx_addr);
        {
            uint8_t q[16] = {0};
            q[0] = 2;
            int64_t qb = (int64_t)(uintptr_t)(q);
            acc = ((acc + (q)[0]) + qb);
        }
        {
            uint8_t rx[16] = {0};
            rx[1] = 3;
            acc = (acc + (rx)[1]);
        }
        uint8_t rx[16] = {0};
        rx[2] = 4;
        acc = (acc + (rx)[2]);
        return acc;
    }
}

int64_t curlee_main(void)
{
    {
        return curlee_probe();
    }
}

