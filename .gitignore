build/

# Local clone of the GitHub wiki (kept in-workspace for agents, not tracked).
wiki/

# Private docs/workflow notes: keep locally, not in git.
*.md
!/README.md
!/docs/README.md

# Local prompt / training artifacts.
training_data.txt
