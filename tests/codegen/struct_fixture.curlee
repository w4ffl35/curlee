struct Point
{
    x : Int;
    y : Int;
}

fn main() -> Int
{
    let p : Point = Point{x : 1, y : 2};
    let q : Point = Point{x : 10, y : 20};
    let sum : Int = p.x + p.y + q.x + q.y;
    return sum;
}
