/* Generated by curlee build --target freestanding-c. */
/* Verified Curlee program compiled to freestanding C. */

#include <stdint.h>

typedef struct curlee_Mode curlee_Mode;

enum curlee_Mode_tag {
    curlee_Mode_A = 0,
    curlee_Mode_B = 1,
};

typedef struct curlee_Mode {
    enum curlee_Mode_tag tag;
} curlee_Mode;

void curlee_main(void);

void curlee_main(void)
{
    curlee_Mode m = ((curlee_Mode){ .tag = curlee_Mode_A });
    if ((m).tag == curlee_Mode_A)
    {
        return;
    }
    else if ((m).tag == curlee_Mode_B)
    {
        return;
    }
    return;
}

