/* Generated by curlee build --target freestanding-c. */
/* Verified Curlee program compiled to freestanding C. */

#include <stdint.h>

static int64_t curlee_double(int64_t x);
int64_t curlee_main(void);

static int64_t curlee_double(int64_t x)
{
    return (x * 2);
}

int64_t curlee_main(void)
{
    int64_t a = 5;
    int64_t b = 3;
    _Bool c = (a > b);
    if (c)
    {
        return curlee_double(a);
    }
    else
    {
        return 0;
    }
}

