// SPDX-License-Identifier: MIT
//
// Issue #278 expected-fail: arrays are freestanding-local `let` bindings only
// in the MVP; they cannot be struct fields (no value-copy of arrays yet).
// Expected error:
//   "array types are only supported as local 'let' bindings in the MVP (not as
//    struct fields)"

struct Bad {
  q: [Int; 8];
}

fn main() -> Int {
  return 0;
}
