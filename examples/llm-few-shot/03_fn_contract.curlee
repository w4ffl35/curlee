fn clamp(x: Int, lo: Int, hi: Int) -> Int
  [ requires lo <= hi;
    ensures result >= lo && result <= hi; ]
{
  if (x < lo) {
    return lo;
  } else {
    if (x > hi) {
      return hi;
    } else {
      return x;
    }
  }
}

fn main() -> Int {
  return clamp(15, 0, 10);
}
