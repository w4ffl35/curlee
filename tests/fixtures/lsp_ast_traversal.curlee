struct Point
{
    x : Int;
    y : Int;
}

fn add(a : Int, b : Int) -> Int
{
    return a + b;
}

fn main() -> Int
{
    let p : Point = Point{x : 1, y : 2};
    let x_val : Int = p.x;
    let sum : Int = add(p.x, p.y);
    let tmp : Int = (sum);

    if (tmp > 0) {
        { let z : Int = -tmp; }
    } else {
        while (tmp < 0) {
            unsafe { add(tmp, 1); }
            return 0;
        }
    }

    return x_val + tmp;
}
