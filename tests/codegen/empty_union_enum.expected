/* Generated by curlee build --target freestanding-c. */
/* Verified Curlee program compiled to freestanding C. */

#include <stdint.h>

typedef struct curlee_Status curlee_Status;

enum curlee_Status_tag {
    curlee_Status_Ok = 0,
    curlee_Status_Err = 1,
};

typedef struct curlee_Status {
    enum curlee_Status_tag tag;
} curlee_Status;

int64_t curlee_main(void);

int64_t curlee_main(void)
{
    curlee_Status s = ((curlee_Status){ .tag = curlee_Status_Ok });
    if (((s).tag == curlee_Status_Ok))
    {
        return 1;
    }
    return 0;
}

