/* Generated by curlee build --target freestanding-c. */
/* Verified Curlee program compiled to freestanding C. */

#include <stdint.h>

static int64_t curlee_tool_queue(void);
static int64_t curlee_rx_state(void);
static int64_t curlee_nonzero_repeat(void);
static int64_t curlee_u64_elements(void);
int64_t curlee_main(void);

static int64_t curlee_tool_queue(void)
{
    int64_t q[4] = {0};
    q[0] = 10;
    q[1] = 20;
    int64_t head = 2;
    int64_t count = 2;
    int64_t slots = 4;
    int64_t idx = ((((head + slots) - count)) % slots);
    int64_t v = (q)[idx];
    return v;
}

static int64_t curlee_rx_state(void)
{
    uint8_t rx[16] = {0};
    rx[0] = 1;
    rx[1] = 2;
    uint8_t s0 = (rx)[0];
    uint8_t s1 = (rx)[1];
    if (((s0 == 1) && (s1 == 2)))
    {
        return 0;
    }
    return 1;
}

static int64_t curlee_nonzero_repeat(void)
{
    int64_t a[4] = {7, 7, 7, 7};
    uint16_t b[2] = {0x41, 0x41};
    a[1] = ((a)[0] + 1);
    int64_t v = ((a)[1] + (b)[1]);
    return v;
}

static int64_t curlee_u64_elements(void)
{
    uint64_t u[2] = {0};
    u[0] = 5;
    uint64_t v = (u)[0];
    uint64_t five = 5;
    if ((v == five))
    {
        return 0;
    }
    return 1;
}

int64_t curlee_main(void)
{
    return (((curlee_tool_queue() + curlee_rx_state()) + curlee_nonzero_repeat()) + curlee_u64_elements());
}

